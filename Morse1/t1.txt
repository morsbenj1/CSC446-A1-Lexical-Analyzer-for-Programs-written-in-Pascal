program TestProgram;
var x, y : integer;
begin
    x := 5;
    y := 10;
    if x < y then
        writeln('x is less than y');
    else
        writeln('x is greater than or equal to y');
end.
